import React from 'react'

const TotalFormat = (props) => {

    // const getData

    return (
        <div>
            <h2>TODAY'S TOTAL REVENUE IS {props.postData.amount}</h2>
        </div>
    )
}

export default TotalFormat